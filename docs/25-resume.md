# 25 — Resume: blocker RESOLVED — the full library boots on the Quadra 800

Paste into a fresh session to continue. **State: the docs/24 "rb-cli HFS catalog
corruption" blocker was NOT an rb-cli HEAD bug — the build was silently running a
STALE pre-fix rb-cli from `$PATH`. HEAD `1b22518` builds the full library
fsck-clean (16,680 files, 0 errors), and that disk now BOOTS + navigates on the
q800. MacAtrium is hardened so the stale-binary trap can't recur.** Supersedes docs/24.

## 0. CRITICAL environment facts (don't re-learn the hard way)

Build AND all emulators are LOCAL on this box (memory `build-and-snow-are-local`):
- **Compile the 68k launcher:** `cmake --build build` (Retro68 `~/repos/Retro68-build`),
  re-embeds to `tools/atrium-tool/assets/MacAtrium.bin`. Then rebuild atrium-tool
  (`cargo build --release --manifest-path tools/atrium-tool/Cargo.toml`).
- **Snow (Mac II, 68020, ≤8-bit):** `~/repos/snow`; harness
  `tools/snow-harness/macatrium_harness`. For B&W / colour variants.
- **QEMU q800 (Quadra 800, 68040) — 7.5.5 / 24-bit / full library:**
  `tools/qemu-harness/q800_harness.py`. ROM extract from `~/repos/mame/roms/macqd800.zip`
  → `/tmp/q800rom/f1acad13.rom` (crc32 `4e70e3c0`). **Gotcha:** the QMP socket is
  `<out>/qmp.sock`; a deep out-dir overflows the ~108-char `AF_UNIX` limit → use a
  SHORT out dir (e.g. `/tmp/q8r`). `-snapshot` so the disk is never mutated.
- **rb-cli** lives at `/home/dani/repos/rusty-backup/target/release/rb-cli` (HEAD
  `1b22518`). **`$PATH`'s rb-cli (`~/.local/bin/rb-cli`) is NOW the HEAD build too**
  (the maintainer swapped it; old pre-fix one at `…/rb-cli.06-26.bak`). **DO NOT
  rebuild rb-cli — the maintainer manages it.**
- `~/.macatrium.json` has `rb_cli: "rb-cli"` (a BARE name). Builds set the absolute
  path in the build config; the hardened tool now makes that win (see §2). The
  build now logs `[rb-cli] <resolved path> (version)` at start — glance at it.
- Commit straight to main (memory `commit-directly-to-main`).

## 1. What docs/24's blocker actually was (root cause)

The full build's `IndexSiblingLinkBroken` / "no free B-tree nodes" corruption was
**never generated by rb-cli HEAD**. MacAtrium's `RbCli` resolved the binary with
`settings.rb_cli.unwrap_or(cfg.rb_cli)`, so the bare `settings.rb_cli="rb-cli"`
**overrode** the build config's absolute path and was resolved against `$PATH` at
exec time → it ran the **06-26 pre-fix** `~/.local/bin/rb-cli` (0
`btree_try_rotate_leaf` symbols; HEAD has 4). Proof from the maintainer (same
build, identical inputs, only the binary differs):

| rb-cli | result |
|---|---|
| `~/.local/bin/rb-cli` (06-26, pre-fix) | dies at 13,474 files — 415 `IndexSiblingLinkBroken` |
| HEAD `1b22518` | **16,680 files, fsck 0 errors** → `/home/dani/repro-HEAD.hda` |

(Aside the maintainer corrected: rb-cli writes each fork as a single contiguous
extent or fails — never multi-extent, so large forks were never the trigger.)

## 2. DONE + committed this session

- `5588f5b` **rbcli hardening** so the stale-binary trap can't recur:
  - `rbcli::resolve_bin` — per-build `cfg.rb_cli` (absolute) wins over a bare
    `settings.rb_cli`; an absolute path always beats a bare name. Unit-tested.
  - `rbcli::log_version` — every build logs the *resolved* rb-cli path + `--version`
    up front. (Both binaries report `0.1.0`, so the resolved PATH is the
    disambiguator — printing it would have caught this in seconds.)
  - `image.rs run()/add_to_disk()` resolve the binary ONCE and thread that one
    path through `RbCli` AND every `harvest`/`catalog` call (those took
    `&cfg.rb_cli` directly before — the inconsistency that let app injection run
    stale while catalog inject ran HEAD).
- **Full-library q800 boot-verify** (the milestone — first complete library ever
  booted). Booted `/home/dani/repro-HEAD.hda`; auto-launched MacAtrium; paged
  catalog navigated across categories at scale **Recommended 1/4 → Adventure 1/51
  → Action & Arcade 4/85** with on-demand page loads and **no Type-28 crash**;
  **COLOR covers + thumbnails render** (came up at colour/8-bit by default).
  Screenshots: `/home/dani/q800-fulllib-shots/` (also `/tmp/q8r/` until reboot).
- Retracted the rusty-backup handoff doc (banner + nit fixes):
  `/home/dani/repos/rusty-backup/PROMPT-hfs-catalog-repro-recipe.md` — **left
  UNCOMMITTED in their tree for the maintainer** (we don't commit rusty-backup).

Working tree (MacAtrium) is CLEAN after `5588f5b`.

## 3. Variants status

| Variant | base | art | partition | emulator | status |
|---|---|---|---|---|---|
| Mac Plus/SE 6.0.8 (B&W) | 6.0.8 | 1 | 512/384 | Snow | built+verified |
| Mac LC/II 7.1 (Colour) | 7.1 | 1,8 @384px | 1024/768 | Snow+MDC | built+verified |
| Mac II 7.1 (Millions) | 7.1 | 1,8,24 | 3584/3072 | Snow(8-bit)/QEMU | 24-bit art untested |
| **Quadra 800 7.5.5 (full)** | 7.5.5 | 1,8,24 @448px | 3584/3072 | **q800** | **BUILT clean (repro-HEAD.hda) + BOOTS + navigates ✅** |

## 4. The full-build config (recreate it; scratchpad is session-local)

```json
{ "base_os": "7.5.5", "out": "/home/dani/macatrium-755-full.hda",
  "selection": { "mode": "all" }, "art_depths": ["1","8","24"],
  "app_mem_kb": [3584,3072], "max_art_size": "448x448", "disk_size_mb": 2000,
  "mg_archive": "/home/dani/macgarden-archive",
  "rb_cli": "/home/dani/repos/rusty-backup/target/release/rb-cli" }
```
Run: `./tools/atrium-tool/target/release/atrium image --config <that>`. The hardened
tool now uses that absolute `rb_cli` (logs it). NOTE: a rebuild is optional —
`repro-HEAD.hda` is already the clean full disk. Disk is tight (~23 G free); a
fresh build needs the 2 GB output + several GB of `.hqx` staging.

## 5. NEXT (in order)

1. **24-bit "Millions" at full scale on the q800** — boot `repro-HEAD.hda` and push
   Settings→Color Depth→Millions, navigate, confirm the `[3584,3072]` partition
   holds the ~1.4 MB `.24.pict` covers without a Type-28. (This session only
   exercised the default colour/8-bit depth; 24-bit at full scale is still computed,
   not run.) Harness keys: `ret` launches, `esc` menu — drive into Settings.
2. (Optional) re-run §4 to mint the canonical artifact with the hardened tool +
   confirm the `[rb-cli]` log shows the rusty-backup path; only if disk allows.
3. Backlog: **multi-volume library** (docs/23, [[multi-volume-library-backlog]]) —
   >2 GB / >65 k files / full-res art needs boot+metadata vol ≤2 GB + N data vols.
4. Backlog: **MacPack↔MacGarden corroboration** — only ~537/1489 titles install (no
   donor source); enlarge the real library (`macpack-vs-macgarden-corroboration`).
5. Cosmetic: 283 art `<id>.shot.24.pict` names exceed HFS 31 chars → rb-cli rejects
   → those covers silently skipped; shorten art ids on write.

## Memory to read
`rbcli-hfs-catalog-btree-scaling` (now RESOLVED — the stale-$PATH lesson),
`qemu-q800-harness`, `build-and-snow-are-local`, `color-art-memory-budget`,
`multi-volume-library-backlog`, `commit-directly-to-main`,
`workflow-verify-in-emulator`.
