/* Generated by CMake from version.h.in — do not edit. The build stamps the
 * current git commit (short hash, "-dirty" if the tree is modified) so the
 * launcher can show which build it is (the Menu / ESC screen). */
#ifndef MACATRIUM_VERSION_H
#define MACATRIUM_VERSION_H
#define MACATRIUM_VERSION "@MACATRIUM_GIT_HASH@"
#endif
